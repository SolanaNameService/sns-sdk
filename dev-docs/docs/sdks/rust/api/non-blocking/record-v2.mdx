---
displayed_sidebar: docsSidebar
title: Record V2 reads
description: Read current SNS record accounts with the non-blocking Rust SDK client.
---

Record V2 APIs derive and query current SNS record accounts through a non-blocking Solana RPC client. They accept a full supported `.sns` or `.sol` domain and return a name-record header plus payload when the requested record exists.

## APIs

| API                                                              | Description                                           |
| ---------------------------------------------------------------- | ----------------------------------------------------- |
| [`get_record_v2`](./record-v2/get-record-v2)                     | Reads one requested V2 record.                        |
| [`get_multiple_records_v2`](./record-v2/get-multiple-records-v2) | Reads multiple requested V2 records in request order. |
